{
  "name": "echelis-audio",
  "version": "1.0.0",
  "description": "Echelis Audio - In-ear monitors website",
  "private": true,
  "scripts": {
    "dev": "npx http-server -p 3000",
    "build": "echo 'Static site - no build required'",
    "lint": "echo 'No linting configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
